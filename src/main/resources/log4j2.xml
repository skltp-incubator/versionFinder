<Configuration status="WARN" monitorInterval="30">
  <Properties>
    <Property name="LOG_PATTERN">
      %d %-5p %-30c - %m%n
    </Property>
    <Property name="log.dir">
      /var/log/versionVisualizer
    </Property>
  </Properties>

  <Appenders>
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="${LOG_PATTERN}"/>
    </Console>

    <RollingRandomAccessFile name="RollingRandomAccessFile"
                             fileName="${log.dir}/versionVisualizer.log"
                             filePattern="${log.dir}/versionvisualizer.log.%i">
      <PatternLayout>
        <Pattern>%d %-5p [%t] %-30c - %m%n</Pattern>
      </PatternLayout>
      <Policies>
        <!-- KB, MB -->
        <SizeBasedTriggeringPolicy size="50 MB"/>
      </Policies>
      <DefaultRolloverStrategy fileIndex="min" max="10"/>
    </RollingRandomAccessFile>

  </Appenders>

  <Loggers>
    <AsyncLogger name="se.skltp.visualizer" level="DEBUG"/>

    <AsyncRoot level="WARN">
      <AppenderRef ref="RollingRandomAccessFile"/>
    </AsyncRoot>

  </Loggers>
</Configuration>